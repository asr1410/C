#include <stdio.h>
#include <conio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
    char s1[] = "even";
    char s2[] = "odd";
    char s3[30];
    //printf("the strcmp for s1 and s2 return %d\n", strcmp(s2,s1));
    //strcat function combine  the string second to the first
    //puts(strcat(s1, s2));
    //printf("the length of s1 is %d\n", strlen(s1));
    //printf("the length of s2 is %d\n", strlen(s2));
    //printf("the reverse string of s1 is : %s\n", strrev(s1));
    //puts(strrev(s1));
    //printf("the reverse string of s2 is : %s\n", strrev(s2));
    //puts(strrev(s2));
    //strcpy(s3, strcat(s1, s2));
    //puts(s3);
    //puts(s1);
    //puts(s2);
    return 0;
}