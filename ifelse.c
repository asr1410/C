#include <stdio.h>
#include <conio.h>
int main()
{
    if (3 < 2)
    {
        printf("this will execute\n");
    }
    else if (1 > 2)
    {
        printf("the given statement is false");
    }
    else
    {
        printf("the given statement is unknown");
    }
    return 0;
}
